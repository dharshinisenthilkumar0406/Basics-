#include<stdio.h>
int main(){
    int a;
    printf("Input: ");
    scanf("%d",&a);
    if(a%4==0 && a%100!=0){
        printf("Leap Year");
    }
    else if(a%400==0) {
        
            printf("Leap Year");
            
    }
    else{
                printf("Not Leap Year");
            }
    return 0;
    
}
